"use client";
import { Plus, Trash2 } from "lucide-react"; import { minutesLabel } from "@/lib/utils";
export type PlayBlock = { startedAt: string; endedAt: string };
const initial = () => { const day = new Date().toISOString().slice(0, 10); return { startedAt: `${day}T19:00`, endedAt: `${day}T22:00` }; };
export function PlayBlocksEditor({ value, onChange }: { value: PlayBlock[]; onChange: (blocks: PlayBlock[]) => void }) { const minutes = value.reduce((total, b) => total + Math.max(0, (new Date(b.endedAt).getTime() - new Date(b.startedAt).getTime()) / 60000), 0); function update(index: number, key: keyof PlayBlock, text: string) { const next = [...value]; next[index] = { ...next[index], [key]: text }; onChange(next); } return <div className="space-y-3"><div className="flex items-center justify-between"><label className="text-sm font-medium">플레이 일정</label><button type="button" className="btn-quiet py-1 text-xs" onClick={() => onChange([...value, initial()])}><Plus size={14}/> 블록 추가</button></div>{value.map((block, index) => { const duration = new Date(block.endedAt).getTime() - new Date(block.startedAt).getTime(); return <div className="rounded-lg border p-3" key={index}><div className="grid gap-2 sm:grid-cols-2"><input type="datetime-local" value={block.startedAt} onChange={e => update(index, "startedAt", e.target.value)}/><input type="datetime-local" value={block.endedAt} onChange={e => update(index, "endedAt", e.target.value)}/></div><div className="mt-2 flex justify-between text-xs text-steam-mist/70"><span className={duration <= 0 ? "text-red-300" : ""}>{duration > 0 ? minutesLabel(Math.round(duration / 60000)) : "종료 시간이 시작 시간보다 빨라야 합니다"}</span>{value.length > 1 && <button type="button" onClick={() => onChange(value.filter((_, i) => i !== index))}><Trash2 size={15}/></button>}</div></div>; })}<p className="rounded-lg bg-steam-ink/60 p-3 font-bold text-steam-blue">총 {minutesLabel(Math.round(minutes))} · {value.length}일</p></div>; }
