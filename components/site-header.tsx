import Link from "next/link"; import { Gamepad2, Shield } from "lucide-react"; import { ThemeToggle } from "./theme-toggle"; import { getViewer } from "@/lib/auth";
export async function SiteHeader() { const { user, profile } = await getViewer(); return <header className="sticky top-0 z-20 border-b border-white/10 bg-steam-ink/85 backdrop-blur"><nav className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4"><Link className="flex items-center gap-2 font-bold text-white" href="/"><Gamepad2 className="text-steam-blue"/> Party Clear</Link><div className="flex items-center gap-3 text-sm">{profile?.status === "approved" && <><Link href="/dashboard">대시보드</Link><Link href="/sessions">세션</Link><Link href="/stats">통계</Link></>}{profile?.role === "admin" && <Link href="/admin" title="관리자"><Shield size={17}/></Link>}{user ? <Link className="btn-quiet py-1.5" href="/profile">{profile?.display_name ?? "프로필"}</Link> : <Link className="btn py-1.5" href="/login">로그인</Link>}<ThemeToggle /></div></nav></header>; }
