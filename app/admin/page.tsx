import { requireAdmin } from "@/lib/auth"; import { AdminMembers } from "@/components/admin-members";
export default async function AdminPage() { const { supabase } = await requireAdmin(); const { data: members } = await supabase.from("profiles").select("id,display_name,avatar_url,status,role").order("created_at", { ascending: false }); return <><div className="page-heading"><p className="eyebrow">Club operations</p><h1>관리자 패널</h1></div><AdminMembers initial={members ?? []}/></>; }
