"use client";
import { createClient } from "@/lib/supabase/client"; import { useState } from "react"; import { toast } from "sonner";
export default function LoginPage() { const [loading, setLoading] = useState(false); async function signIn() { setLoading(true); const { error } = await createClient().auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${location.origin}/auth/callback` } }); if (error) { toast.error(error.message); setLoading(false); } } return <main className="grid min-h-screen place-items-center p-4"><section className="panel w-full max-w-md text-center"><p className="text-4xl">🎮</p><h1 className="mt-4 text-2xl font-bold text-white">Party Clear에 입장</h1><p className="mt-2 text-sm text-steam-mist/75">Google 계정으로 로그인하세요. 신규 멤버는 관리자 승인 후 이용할 수 있습니다.</p><button onClick={signIn} disabled={loading} className="btn mt-7 w-full">{loading ? "연결 중..." : "Google로 계속하기"}</button></section></main>; }
