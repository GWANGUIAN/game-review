import Link from "next/link"; import { getViewer } from "@/lib/auth";
export default async function PendingPage() { const { profile } = await getViewer(); const rejected = profile?.status === "rejected"; return <main className="grid min-h-screen place-items-center p-4"><section className="panel max-w-md text-center"><p className="text-4xl">{rejected ? "🚫" : "⏳"}</p><h1 className="mt-4 text-2xl font-bold text-white">{rejected ? "가입이 거절되었습니다" : "승인 대기 중입니다"}</h1><p className="mt-2 text-steam-mist/80">{rejected ? "관리자에게 문의해 주세요." : "관리자가 승인하면 바로 파티에 참여할 수 있어요."}</p><Link href="/" className="btn-quiet mt-6">홈으로</Link></section></main>; }
