import Link from "next/link"; import { CircleAlert, Clock3 } from "lucide-react"; import { getViewer } from "@/lib/auth";
export default async function PendingPage() { const { profile } = await getViewer(); const rejected = profile?.status === "rejected"; return <main className="grid min-h-screen place-items-center p-4"><section className="w-full max-w-md border border-white/10 bg-steam-card p-7 text-center shadow-steam"><div className="mx-auto grid h-11 w-11 place-items-center border border-steam-blue/60 text-steam-blue">{rejected ? <CircleAlert size={21}/> : <Clock3 size={21}/>}</div><p className="eyebrow mt-5">Membership status</p><h1 className="mt-2 text-2xl font-bold text-white">{rejected ? "가입이 거절되었습니다" : "승인 대기 중입니다"}</h1><p className="mt-3 text-sm leading-6 text-steam-mist/75">{rejected ? "관리자에게 문의해 주세요." : "관리자가 승인하면 바로 동아리 활동에 참여할 수 있습니다."}</p><Link href="/" className="btn-quiet mt-7">아카이브로 돌아가기</Link></section></main>; }
